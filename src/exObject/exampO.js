function mathTest() {
    let power = Math.pow(2,3)

    let PI = Math.PI;
    let name = new String("Jan");
    // ctrl + p - skrót do podlądu sposobu wprowadzania parametrów obiektu
    let date = new Date();
    console.log(date.getFullYear());
}
